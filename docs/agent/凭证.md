# 凭证信息

## 云端测试账号

用于 https://chuangcut.zeabur.app 的登录测试：

| 字段 | 值 |
|------|-----|
| 用户名 | `xiangyugongzuoliu@gmail.com` |
| 密码 | `<联系管理员获取>` |

## Zeabur 控制台

| 项目 | 值 |
|------|-----|
| Project ID | `69417cec5b09e8f620b1a1c7` |
| Service ID | `69417cec5b09e8f620b1a1c8` |
| Environment ID | `69417cec4947dd57c4fd0167` |
| 线上域名 | https://chuangcut.zeabur.app |
| 控制台 URL | https://zeabur.com/projects/69417cec5b09e8f620b1a1c7/services/69417cec5b09e8f620b1a1c8?envID=69417cec4947dd57c4fd0167 |

## Docker Hub

| 项目 | 值 |
|------|-----|
| 镜像仓库 | https://hub.docker.com/r/xiangyugongzuoliu/chuangcut-video-workflow |
| 镜像标签 | `xiangyugongzuoliu/chuangcut-video-workflow:latest` |

## GitHub 仓库

| 仓库 | 类型 | 用途 |
|------|------|------|
| `xiangyugongzuoliu/chuangcut-video-workflow` | 私有 | 完整源代码 |
| `xiangyugongzuoliu/chuangcut-video-workflow-zeabur` | 公开 | Zeabur 模板资源 |

## GitHub 账号（本项目使用）

本项目位于 `code-xy` 目录，使用 `xiangyugongzuoliu` 账号。

| 字段 | 值 |
|------|-----|
| 用户名 | `xiangyugongzuoliu` |
| Personal Access Token | `ghp_xxx...xxx`（见 `~/.claude/credentials/github-accounts.md`） |
| SSH Host 配置 | `github.com-xiangyugongzuoliu` |
| SSH Key 路径 | `~/.ssh/id_ed25519` |

## 本地开发密钥

以下密钥用于本地开发环境（`.env.local`）：

### 授权系统

| 变量 | 值 | 说明 |
|------|-----|------|
| `LICENSE_KEY` | `CCUT-xxxxxxxxxx-xxxx` | 测试授权码 |
| `LICENSE_ENCRYPTION_KEY` | `<64位字符串>` | 授权文件解密密钥 |

### 加密系统

| 变量 | 值 | 说明 |
|------|-----|------|
| `ENCRYPTION_KEY` | `<64位十六进制>` | API 密钥加密 |
| `SESSION_SECRET` | `<64位十六进制>` | Session 签名密钥 |

### 数据库

| 变量 | 值 |
|------|-----|
| `DATABASE_URL` | `file:./data/db.sqlite` |

### 目录配置

| 变量 | 值 |
|------|-----|
| `TEMP_DIR` | `./temp` |
| `OUTPUT_DIR` | `./output` |

---

## 外部服务 API 密钥

以下密钥通过 `/settings` 页面配置，实际值存储于 `~/.claude/credentials/` 目录。

### Google Gemini AI Studio

| 配置项 | 值 |
|--------|-----|
| API Key | `AIzaSy...`（见 `~/.claude/credentials/google-gemini.md`） |

### Google Gemini Vertex AI

| 配置项 | 值 |
|--------|-----|
| Project ID | `xiangyugongzuoliu` |
| Location | `us-central1`（Gemini 3 使用 `global`） |

**Service Account JSON**：见 `~/.claude/credentials/google-gemini.md`

### Fish Audio

| 配置项 | 值 |
|--------|-----|
| API Key | `<见 ~/.claude/credentials/fish-audio.md>` |
| Voice ID | `780716873dd0423a8568d82aeb17aa7c` |

---

## 测试视频素材

| 用途 | URL |
|------|-----|
| 单视频剪辑 | `https://pub-b65afb21c951453a872a026d19411abe.r2.dev/hunjian-1.mp4` |
| 多视频混剪-视频2 | `https://pub-b65afb21c951453a872a026d19411abe.r2.dev/hunjian-2.mp4` |

---

## Zeabur CLI

| 项目 | 值 |
|------|-----|
| CLI 路径 | `/Users/xiangyu-server/.local/bin/zeabur` |

### 常用命令

```bash
# 设置 Context
zeabur context set project --id 69417cec5b09e8f620b1a1c7 -y -i=false
zeabur context set env --id 69417cec4947dd57c4fd0167 -y -i=false

# 重新部署
zeabur service restart --id 69417cec5b09e8f620b1a1c8 --env-id 69417cec4947dd57c4fd0167 -y -i=false

# 查看日志
zeabur deployment log --service-id 69417cec5b09e8f620b1a1c8 --env-id 69417cec4947dd57c4fd0167 -i=false | head -20

# 查看服务状态
zeabur service list --env-id 69417cec4947dd57c4fd0167 -i=false
```
